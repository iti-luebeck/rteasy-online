# The RTeasy-Online CLI

The CLI is a console-based frontend built on top of the backend.

## Build

`cd` into this directory and then execute `cargo build`.

## Test

`cd` into this directory and then execute `cargo test`.
